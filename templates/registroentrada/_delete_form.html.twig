<form method="post" action="{{ path('app_registroentrada_delete', {'id': registroentrada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registroentrada.id) }}">
    <button class="btn">Delete</button>
</form>
