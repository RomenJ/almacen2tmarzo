<form method="post" action="{{ path('app_objetosalmacen_delete', {'id': objetosalmacen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objetosalmacen.id) }}">
    <button class="btn">Delete</button>
</form>
